{
  "name": "code-quest",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "engines": { "node": ">=18.19.0" },
  "scripts": {
    "test": "vitest run",
    "dev": "npm run dev -w @code-quest/server",
    "build": "npm run build -w @code-quest/web",
    "start": "npm run dev -w @code-quest/server"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
